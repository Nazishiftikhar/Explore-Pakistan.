@model IEnumerable<Tour_Plan_Agency.Models.tblWebService>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.WServices_Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.WServices_Price)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.WServices_Description)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.WServices_Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.WServices_Price)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.WServices_Description)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.WServices_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.WServices_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.WServices_ID })
        </td>
    </tr>
}

</table>
